[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

[[redirects]]
  from = "/*"
  to = "/:splat"
  status = 200

# Проверка предстоящих звонков и отправка push-уведомлений — каждые 5 минут
[functions."check-reminders"]
  schedule = "*/5 * * * *"
